<Page x:Class="PRSTV_WPF.Pages.CountMenu"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      FontSize="16"
      Title="Count Menu">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header -->
            <RowDefinition Height="*"/>
            <!-- Election list -->
            <RowDefinition Height="Auto"/>
            <!-- Action buttons -->
        </Grid.RowDefinitions>

        <!-- PAGE HEADER -->
        <TextBlock Grid.Row="0"
                   Text="Count Menu"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Margin="0,0,0,12"/>

        <!-- ELECTION LIST -->
        <Border Grid.Row="1"
                CornerRadius="8"
                BorderBrush="#DDD"
                BorderThickness="1"
                Padding="8">

            <ListView x:Name="ElectionsList"
                      SelectionMode="Single">

                <!-- Placeholder empty state -->
                <ListView.Items>
                    <!-- Intentionally empty for now -->
                </ListView.Items>

                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Election Name"
                                        Width="300"/>
                        <GridViewColumn Header="Status"
                                        Width="160"/>
                        <GridViewColumn Header="Last Updated"
                                        Width="200"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>

        <!-- ACTION BUTTONS -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">

            <Button Content="Continue Count"
                    Padding="16,8"
                    Margin="0,0,10,0"
                    Click="ContinueCount_Click"/>

            <Button Content="Start Election"
                    Padding="16,8"
                    Margin="0,0,10,0"
                    Click="StartElection_Click"/>

            <Button Content="Resolve Doubtful"
                    Padding="16,8"
                    Click="ResolveDoubtful_Click"/>
        </StackPanel>
    </Grid>
</Page>
