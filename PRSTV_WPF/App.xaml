<Application x:Class="PRSTV_WPF.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:PRSTV_WPF"
             StartupUri="MainWindow.xaml">
    <Application.Resources>

        <!-- Theme primitives -->
        <SolidColorBrush x:Key="Brush.WindowBackground" Color="#F7F8FA" />
        <SolidColorBrush x:Key="Brush.CardBackground" Color="White" />
        <SolidColorBrush x:Key="Brush.Border" Color="#D9DDE3" />
        <SolidColorBrush x:Key="Brush.TextPrimary" Color="#1F2937" />
        <SolidColorBrush x:Key="Brush.TextSecondary" Color="#6B7280" />
        <SolidColorBrush x:Key="Brush.Primary" Color="#2563EB" />
        <SolidColorBrush x:Key="Brush.PrimaryHover" Color="#1D4ED8" />
        <SolidColorBrush x:Key="Brush.PrimaryPressed" Color="#1E40AF" />
        <SolidColorBrush x:Key="Brush.Disabled" Color="#E5E7EB" />
        <SolidColorBrush x:Key="Brush.Danger" Color="#DC2626" />

        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <!-- Base typography -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}" />
        </Style>

        <!-- Card -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource Brush.CardBackground}" />
            <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="12" />
            <Setter Property="Padding" Value="16" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
        </Style>

        <!-- TextBox -->
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="40" />
            <Setter Property="Padding" Value="10,0" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="White" />
            <Setter Property="Margin" Value="0,6,0,0" />
        </Style>

        <!-- Button base template with corner radius + states -->
        <Style TargetType="Button">
            <Setter Property="Height" Value="42" />
            <Setter Property="MinWidth" Value="160" />
            <Setter Property="Padding" Value="16,0" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
						Background="{TemplateBinding Background}"
						BorderBrush="{TemplateBinding BorderBrush}"
						BorderThickness="{TemplateBinding BorderThickness}"
						CornerRadius="10"
						SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.95" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.9" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource Brush.Disabled}" />
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Brush.Border}" />
                                <Setter Property="Foreground" Value="{StaticResource Brush.TextSecondary}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary / Secondary -->
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource Brush.Primary}" />
            <Setter Property="BorderBrush" Value="{StaticResource Brush.Primary}" />
            <Setter Property="Foreground" Value="White" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource Brush.PrimaryHover}" />
                    <Setter Property="BorderBrush" Value="{StaticResource Brush.PrimaryHover}" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{StaticResource Brush.PrimaryPressed}" />
                    <Setter Property="BorderBrush" Value="{StaticResource Brush.PrimaryPressed}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}" />
            <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}" />
        </Style>

        <!-- Destructive (red) action -->
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource Brush.Danger}" />
            <Setter Property="BorderBrush" Value="{StaticResource Brush.Danger}" />
            <Setter Property="Foreground" Value="White" />
        </Style>

        <!-- DataGrid readability -->
        <Style TargetType="DataGrid">
            <Setter Property="RowHeight" Value="34" />
            <Setter Property="AlternationCount" Value="2" />
            <Setter Property="GridLinesVisibility" Value="None" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="CanUserAddRows" Value="False" />
            <Setter Property="CanUserDeleteRows" Value="False" />
            <Setter Property="CanUserResizeRows" Value="False" />
            <Setter Property="IsReadOnly" Value="True" />
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="White" />
            <Style.Triggers>
                <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                    <Setter Property="Background" Value="#F9FAFB" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Setter Property="Background" Value="#F3F4F6" />
        </Style>

    </Application.Resources>
</Application>
